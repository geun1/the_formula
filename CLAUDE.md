@AGENTS.md

# Code Style

- TypeScript only — no plain `.js` files.
- Use named exports, not default exports.
- No `console.log` in production code — use the project's existing logger or omit entirely.
